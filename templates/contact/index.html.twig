{% extends 'base.html.twig' %}

{% block title %}Prendre contact - Beemymate{% endblock %}


{% block body %}

<main id="contact">
    <section class="contain">
       
       <article>
            <h1>Contact <span class="couleur">nous !</span></h1>
            <p>Un logement d'intéresse ?</p>
            <p>Tu as des questions concernant Beemymate ?</p>
            <p>Et si nous en parlions ?<br>Tu visualiseras ainsi comment Beemymate te soutiendra et t’accompagnera dans tes recherches d’appartemen</p>
            <p>Pose toutes tes questions avec le formulaire... ou contacte-nous par mail.</p>
            <p>Au plaisir d’échanger avec toi !</p>
       </article>

        <article>
            {{ form_start(contactForm) }}
                <div class="nom">
                    {{ form_row(contactForm.prenom)}}
                    {{ form_row(contactForm.nom) }}
                </div>
                {{ form_row(contactForm.email) }}
                {{ form_row(contactForm.objet) }}
                {{ form_row(contactForm.message) }}
                <button type="submit" class="btn">Envoyer</button>
            {{ form_end(contactForm) }}

            {% for message in app.flashes('success') %}
                <p>{{ message }}</p>
            {% endfor %}
        </article>
    </section>
</main>
{% endblock %}
